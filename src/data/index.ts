/**
 * src/data/index.ts
 *
 * Barrel export for all mock/seed data modules.
 * This project is frontend-only — all data lives here as static TypeScript.
 *
 * Example structure:
 *   export { employees } from './employees'
 *   export { payrollRuns } from './payrollRuns'
 *   export { departments } from './departments'
 *   export { leaveRequests } from './leaveRequests'
 */
