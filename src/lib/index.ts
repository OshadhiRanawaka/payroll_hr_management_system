/**
 * src/lib/index.ts
 *
 * Barrel export for shared utilities, helpers, and library wrappers.
 * Add exports here as you create them, e.g.:
 *
 *   export * from './formatters'
 *   export * from './dateUtils'
 *   export * from './cn'           // className merge helper
 */
